<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login In Page</title>
    <link
    href="https://cdn.jsdelivr.net/npm/remixicon@4.5.0/fonts/remixicon.css"
    rel="stylesheet"
/>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-b from-white to-gray-100 flex justify-center items-center min-h-screen">
    <div class="bg-white p-6 rounded-2xl shadow-lg w-full  h-00  max-w-xs">
       
        <div class="flex justify-center mb-6">
            <img src="/media/SISTec-logo.png" alt="SISTec Logo" class="w-40">
        </div>

        
        <h2 class="text-xl font-bold text-center mb-4">Login in</h2>

        
        <form action="/login" method="post">
         
            <div class="mb-4">
                <label for="email" class="sr-only">Email</label>
                <div class="relative">
                    <input type="email"  name="email" id="email" placeholder="abc@email.com" class="w-full border border-gray-300 rounded-lg px-4 py-2 pl-10 text-gray-700 focus:ring-2 focus:ring-blue-500 focus:outline-none">
                    <div class="absolute inset-y-0 left-0 flex items-center pl-3">
                        <i class="ri-mail-line"></i>

                       
                    </div>
                </div>
            </div>

            <div class="mb-4">
                <label for="password" class="sr-only">Password</label>
                <div class="relative">
                    <input type="password" name="password"  id="password" placeholder="Your password" class="w-full border border-gray-300 rounded-lg px-4 py-2 pl-10 text-gray-700 focus:ring-2 focus:ring-blue-500 focus:outline-none">
                    <div class="absolute inset-y-0 left-0 flex items-center pl-3">
                        <i class="ri-lock-line"></i> 
                    </div>
                </div>
            </div>

           
            <div class="flex items-center justify-between mb-4">
                <div class="flex items-center">
                    <input id="remember" type="checkbox" class="h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 rounded">
                    <label for="remember" class="ml-2 block text-sm text-gray-900">Remember Me</label>
                </div>
                <a href="#" class="text-sm text-blue-600 hover:underline">Forgot Password?</a>
            </div>

            
            <button type="submit" class="w-full bg-blue-500 text-white font-semibold py-2 rounded-lg hover:bg-blue-600 focus:ring-2 focus:ring-blue-500 focus:outline-none">Sign In</button>
        </form>
    </div>
</body>
</html>
